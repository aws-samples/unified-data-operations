product:
  id: product_b_sales
  version: "1.0.0"
  description: Sales
  pipeline:
    schedule: "0 3 * * *"
    tasks:
      - id: extract_sales_orders
        logic:
          func: tasks.ingest
        input:
          - id: sales_order
            type: connection
            connection_id: test_db_connection
            table: dms_sample.sporting_event_ticket_int
            model: sales_order
