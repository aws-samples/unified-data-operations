product:
  id: customers
  pipeline:
    id: ingest data
    tasks:
      - id: extract_customers
        seq: 1
        logic: tasks.custom_business_logic
        input:
          - id: person
            type: connection
            connection_id: test_db_connection
            table: dms_sample.person
            model: person

