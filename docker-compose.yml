version: "2.3"
services:
  test:
    image: job-interpreter-env:dev
    build:
      context: .
    volumes:
      - ./deprecated:/app/deprecated
      - ./main.py:/app/main.py
    command: ["pytest", "-s", "-m", "not integration"]
